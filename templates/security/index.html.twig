{% extends 'base.html.twig' %}

{% block body %}
    <main class="container">
        <section id="account-registration" class="card">
            <h4 class="card-header">Créez votre compte</h4>
            <p style="margin: 2em"> Vous avez déjà un compte?<a href="{{ path('login') }}"> Connectez-vous!</a></p>
            <div class="row justify-content-center">
                <div class="col-md-7 col-11">
                    {{ form(form) }}
                </div>
            </div>
        </section>
    </main>
{% endblock %}
