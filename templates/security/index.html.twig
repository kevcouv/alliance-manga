{% extends 'base.html.twig' %}

{% block body %}
    <main class="container">
        <section id="account-registration" class="card">
            <h4 class="card-header">Créez votre compte</h4>
            <p style="margin: 2em"> Vous avez déjà un compte?<a href="#" data-toggle="modal"  data-target="#login"> Connectez-vous!</a></p>
            <div class="row justify-content-center">
                <div class="col-md-7 col-11">
                    {{ form(form) }}
                </div>
            </div>
        </section>
        {{ render(controller('App\\Controller\\SecurityController::login')) }}
    </main>
{% endblock %}
